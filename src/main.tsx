import React from "react";
import ReactDOM from "react-dom/client";
import "@molb-prelogin/zero-design-library/styles";
import "./widget.css";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
